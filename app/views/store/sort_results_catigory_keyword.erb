<h2 class="search">Sort Results Based On Key Word By Category</h2>

<% if @products.empty? %>
<p class="search">
	Sorry, there are no products in the '<%= params[:keywords] %>' category.
</p>
<% end %>

<%= render @products %>
</br>
<h4 class="results">
	<%= link_to 'Home', root_path %>
</h4>
